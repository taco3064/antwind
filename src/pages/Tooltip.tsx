import Arrow from '~demo/tooltip/arrow';
import Placement from '~demo/tooltip/placement';
import { DemoContainer } from '~ata/components';

export default function TooltipPage() {
  return <DemoContainer title="Tooltip" items={{ Arrow, Placement }} />;
}
