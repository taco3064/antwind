import Placement from '~demo/popconfirm/placement';
import { DemoContainer } from '~ata/components';

export default function PopconfirmPage() {
  return <DemoContainer title="Popconfirm" items={{ Placement }} />;
}
