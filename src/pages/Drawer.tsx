import Loading from '~demo/drawer/loading';
import Placement from '~demo/drawer/placement';
import { DemoContainer } from '~ata/components';

export default function DrawerPage() {
  return <DemoContainer title="Drawer" items={{ Loading, Placement }} />;
}
