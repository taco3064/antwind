import Arrow from '~demo/popover/arrow';
import Placement from '~demo/popover/placement';
import { DemoContainer } from '~ata/components';

export default function PopoverPage() {
  return <DemoContainer title="Popover" items={{ Arrow, Placement }} />;
}
